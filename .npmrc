# pnpm configuration
auto-install-peers=true
strict-peer-dependencies=false

# Security overrides - Fix esbuild vulnerability (CVE-2025-24565)
# pnpm uses package.json overrides/pnpm.overrides instead of .npmrc
